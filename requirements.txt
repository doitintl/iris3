pyyaml==6.0
Flask==2.2.3
google-api-python-client==2.82.0
google-cloud-pubsub==2.15.2
oauth2client==4.1.3

ratelimit==2.2.1

google-cloud-compute==1.10.1
google-cloud-resource-manager==1.9.0
google-cloud-bigquery==3.7.0
google-cloud-storage==2.7.0

#CloudSQL Admin: In 3-2023, https://cloud.google.com/sql/docs/mysql/admin-api/libraries#python
# Says to use the discovery based  google-api-python-client

#For Google Cloud PRofiler,  set up app.yaml as as below
google-cloud-profiler
# gunicorn is only needed if you add an entrypoint:
# to app.yaml as a workaround to let Cloud Profiler work.
# Do so as follows:
# gunicorn -b :$PORT --worker-class=sync --workers=1 main:app

#gunicorn==19.5.0
