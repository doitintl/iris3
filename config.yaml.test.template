# If you see this comment, you are using a config file that was temporarily
# generated for a test.
#
# In integration_test.sh, config.yaml.test.template is automatically converted to a temporary config.yaml, then reverted.
#
iris_prefix: ${RUN_ID}
from_project: True
projects:
  - ${DEPLOYMENT_PROJECT}
  - ${TEST_PROJECT}
